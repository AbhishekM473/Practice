<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhishek.medsreminder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name=".dbManager.MedsContentProvider"
            android:authorities="com.example.abhishek.medsreminder" />

        <receiver android:process=":remote"
            android:name=".services.MyReceiver" />

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MedicineActivity" />
        <activity android:name=".activities.MedsListActivity" />
        <activity android:name=".activities.AlarmActivity"></activity>
    </application>

</manifest>